{
  "name": "drawie",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "npm install && cd web && npm install && npm run build"
  },
  "author": "Pawel Wieckowski",
  "license": "ISC",
  "dependencies": {
    "canvas": "^1.6.10",
    "express": "^4.16.3",
    "path": "^0.12.7",
    "socket.io": "^2.1.0"
  }
}
